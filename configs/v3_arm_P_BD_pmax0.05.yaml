# Frozen v3 arm: P_BD_pmax0.05 (P/candidate).  Generated by run_v3_campaign.py.
experiment_name: v3_P_BD_pmax0.05
output_root: results/v3/arms/P_BD_pmax0.05
backend: torch
device: cuda
dtype: float64
batch_size_configs: 8

selection:
  write_generic_best: false

potential:
  x_tilt: 0.1021665783

domain:
  x_min: -3.0
  x_max: 3.0
  y_min: -2.5
  y_max: 3.5
  nx_ref: 801
  ny_ref: 801
  nx_profile: 401

simulation:
  beta: 4.0
  dt: 0.002
  n_particles: 256
  n_steps: 50000
  eval_every: 500
  seeds: [0, 1, 2, 3, 4, 5, 6, 7]
  x_init_mode: uniform
  y_init_mode: uniform

abf:
  estimator: binned_smooth
  observation_order: post_propagation
  h: 0.05
  update_every: 10
  min_count: 1.0
  ema_alpha: 0.05

fr:
  enabled: false
  target_types: []
  gamma_values: [0.0]
  eta_values: [0.10]
  burnin_fractions: [0.2]
  duration_fractions: [0.6]
  ramp_fraction: 0.0
  fr_every_values: [1]
  noise_chunk_steps: 1000

v3: {"enabled": true, "name": "P_BD_pmax0.05", "fr_stride": 500, "family": {"kind": "flat"}, "target_family": {"kind": "physical"}, "operator": "bd", "p_max": 0.05}

methods:
  - abf_only
