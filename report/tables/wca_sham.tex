% Auto-generated numbers via tables/wca_sham_numbers.tex; layout maintained by hand.
\begin{table}[t]\centering
\caption{Matched-sham control at the accepted WCA-positive cell (\WcsCell, $M=100$ particles,
\WcsSteps{} steps, $N=\WcsReplicas$, FR rate \WcsRate) on \WcsSeeds{} fresh seeds, with no
hyperparameter reopened. Percentages are paired differences in the integrated free-energy
error against ABF on the same seed; \textbf{negative is better}. Each sham replays its own
partner's per-event replacement counts on that seed and differs only in randomising which
replicas die and which are copied (\WcsMismatch{} count mismatches across all seeds and both
partners). The \emph{direct} rows are the preregistered attribution test.}
\label{tab:wca_sham}
\small
\begin{tabular}{lrrrr}
\toprule
comparison & $\Delta I_F$ & 95\% CI & seeds won & round trips \\
\midrule
\multicolumn{5}{l}{\emph{against ABF}}\\
practical mFR & \textbf{\WcsEstGain\%} & $[\WcsEstCIlo, \WcsEstCIhi]$ & \WcsEstWins &
  \WcsEstRoundTrips \\
sham (practical) & $\WcsShamGain\%$ & $[\WcsShamCIlo, \WcsShamCIhi]$ & --- &
  \WcsShamRoundTrips \\
oracle mFR & \WcsOracleGain\% & $[\WcsOracleCIlo, \WcsOracleCIhi]$ & --- & --- \\
\midrule
\multicolumn{5}{l}{\emph{direct: each arm against its own sham --- the attribution test}}\\
practical mFR vs its sham & \textbf{\WcsDirect\%} & $[\WcsDirectCIlo, \WcsDirectCIhi]$ &
  \WcsDirectWins & --- \\
oracle mFR vs its sham & \WcsOracleDirect\% &
  $[\WcsOracleDirectCIlo, \WcsOracleDirectCIhi]$ & --- & --- \\
\bottomrule
\end{tabular}

\vspace{2pt}
{\footnotesize Health at the practical arm: min ancestor $\mathrm{ESS}/N = \WcsEstEss$ and max
ancestor fraction $\WcsEstAnc$, against preregistered floors of $0.10$ and $0.05$. The looser
ESS floor than the gateway's $0.30$ was declared in advance, because the accepted WCA
configuration already runs at $\mathrm{ESS}/N \approx 0.17$ and the gateway's floor would have
failed the configuration under test rather than tested it.}
\end{table}
