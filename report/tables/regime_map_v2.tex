% v2 regime map. Hand-maintained (NOT generated by build_closure_inventory.py, which reads the
% v1 inventory only). One row per v2 system; the four-regime map with Gate 0 leading.
\begin{table}[p]
\centering
\footnotesize
\caption{Regime map over the four \textbf{v2} systems, under the gate order fixed by Amendment~10:
Gate 0 (is the conditional ensemble at fixed $\xi$ equilibrated?), then A (are the states visible
through the CV?), then B (are they found?), then C (are they populated?). The regime is assigned by
the \emph{first} failing gate, so a row's later columns are blank where the gate was never
evaluated. No system in this table licensed an mFR arm, and none was run. \Cref{tab:regime_map}
covers the eleven v1 benchmarks.}
\label{tab:regime_map_v2}
\newcolumntype{R}[1]{>{\raggedright\arraybackslash}p{#1}}
\setlength{\tabcolsep}{2.5pt}
\begin{tabular}{R{1.7cm}R{1.5cm}R{1.9cm}R{1.9cm}R{2.4cm}R{2.5cm}R{2.3cm}}
\toprule
System & CV & Gate 0 & Gate A & Gate B (discovery) & Gate C (establishment) & Regime \\
\midrule
Deca-alanine \newline (\cref{sec:deca}) & terminal C--C distance & \textbf{FAIL} $\DcGateZero$; persists to $\DcGateZeroCounts$ counts/bin & pass $\DcGateATV$ & \emph{not evaluated} & \emph{not evaluated}; the screen's deficit is voided with the baseline & conditional-equilibration-limited \\
\midrule
Methane pair \newline (\cref{sec:methane}) & \ce{C}--\ce{C} distance & pass $\MeGateZeroGlobal$ & pass $\MeGateAPre$; descriptor $\MeOrthoRatio\times$ orthogonal & pass $\MeGateBhits$, but \emph{vacuous}: domain is $\MeBallisticSteps$ ballistic steps wide & no deficit $\MeGateCdeficit$; worst ratio $\MeOccRatioWorst$ over $\MeCheckpoints$ checkpoints & ABF-sufficient \\
\midrule
NaCl pair \newline (\cref{sec:nacl}) & \ce{Na}--\ce{Cl} distance & pass $\NaGateZeroGlobal$ (campaign best) & pass $\NaGateAPre$, but hydration is nearly a function of $r$ ($\NaOrthoLo$--$\NaOrthoHi\times$) & pass $\NaGateBhits$; the \emph{number} is resolution-limited, the far-threshold statistic carries it & no deficit $\NaGateCdeficit$ on the powered state, both cells; min ratio $\NaDirectHi$ & ABF-sufficient \newline (\emph{weak null}) \\
\midrule
$R_{15}$ audits \newline (\cref{sec:r15_audits}) & end-to-end distance & fails \emph{locally} $\GzRfifteenLo$--$\GzRfifteenHi$ over $[\VrBandLo,\VrBandHi]$~nm, carrying $\VrBandError\,\%$ of error over $\VrBandMask\,\%$ of the mask & pass & \textbf{FAIL}: the state is never reached & \emph{not evaluated} & discovery-limited \newline (local Gate 0 caveat) \\
\bottomrule
\end{tabular}
\end{table}
