% Auto-generated numbers via tables/gateway_numbers.tex; layout maintained by hand.
\begin{table}[t]\centering
\caption{\textbf{Confirmatory} run at the preregistered establishment-limited anchor
($\beta=\GwAnchorBeta$, $s=\GwAnchorS$, $r=\GwAnchorR$): $\GwcSeeds$ fresh seeds, rates
frozen from a separate calibration pass, no ladder and no tuning. Percentages are paired
differences against ABF on the same seed with a bootstrap CI over seeds; \textbf{negative is
better}. Each sham fires at its own partner's event times with its partner's realised
clone/delete counts, and differs only in randomising which walkers they act on. The
\emph{direct} rows are the attribution statistic: each FR arm against its own sham on the same
seed, which holds the schedule and the count fixed so that only the selection direction
differs. Health gates $\mathrm{ESS}_{\mathrm{anc}}/N \ge \GwEssGate$ and
$w_{\max} \le \GwWmaxGate$ pass for every arm shown.}
\label{tab:gw_anchor}
\small
\begin{tabular}{lrrrrr}
\toprule
comparison & $\gamma$ & $\Delta I_F$ & 95\% CI & seeds won & frozen-bias $\Delta$ \\
\midrule
\multicolumn{6}{l}{\emph{against ABF}}\\
practical mFR & \GwEstGamma & \textbf{\GwcEstGain\%} &
  $[\GwcEstCIlo, \GwcEstCIhi]$ & \GwcEstWins & \GwcFrozen\% \\
sham (practical) & \GwEstGamma & \GwcShamGain\% &
  $[\GwcShamCIlo, \GwcShamCIhi]^{\dagger}$ & --- & \GwcShamFrozen\% \\
\midrule
\multicolumn{6}{l}{\emph{direct: each arm against its own sham --- the attribution test}}\\
practical mFR vs sham & \GwEstGamma & \textbf{\GwcDirect\%} &
  $[\GwcDirectCIlo, \GwcDirectCIhi]$ & \GwcDirectWins & \GwcDirectFrozen\% \\
oracle mFR vs sham & \GwOracleGamma & \GwcOracleDirect\% &
  $[\GwcOracleDirectCIlo, \GwcOracleDirectCIhi]$ & --- & --- \\
\midrule
\multicolumn{6}{l}{\emph{mechanism control: one walker seeded across the gateway, so
discovery is free}}\\
practical mFR & \GwEstGamma & \GwcOneRight\% &
  $[\GwcOneRightCIlo, \GwcOneRightCIhi]$ & \GwcOneRightWins & --- \\
\bottomrule
\end{tabular}

\vspace{2pt}
{\footnotesize $^{\dagger}$ 90\,\% CI, the interval for the two-one-sided-tests equivalence
check against a $\pm\GwcMargin\,\%$ margin; the practical sham is \GwcShamTost{} to ABF on
this endpoint. The oracle arm's sham is \GwcShamOracleTost{}
($90\,\%$ CI $[\GwcShamOracleCIlo, \GwcShamOracleCIhi]$), so only the practical arm supports
an equivalence claim.}
\end{table}
