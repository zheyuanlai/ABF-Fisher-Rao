We study whether augmenting the Adaptive Biasing Force (ABF) method with a
Fisher--Rao (FR) birth--death correction improves the computation of a
reaction-coordinate free energy, and what kind of improvement it can reasonably be
expected to provide. We test this on five core case studies (then extended along the
collective variable itself) spanning model potentials,
a condensed-phase dimer, and molecular torsions: a two-dimensional double-well
\emph{metastability} model with a quadrature reference; a two-dimensional
\emph{entropic-bottleneck} model with a narrow transverse channel, for which both
the reference free energy and the conditional law
$Y\mid X=x\sim\mathcal N(0,1/(\beta\omega(x)^2))$ are analytic; a many-body
condensed-phase \emph{Weeks--Chandler--Andersen (WCA) dimer} in a dense solvent,
evaluated against a high-accuracy thermodynamic-integration reference; and two
united-atom alkanes whose slow coordinate is a signed dihedral --- \emph{butane}
(one torsion, an easy control with an exact analytic reference) and \emph{pentane}
(two torsions coupled by the 1--5 ``pentane effect'', so that the second dihedral is
a hidden slow coordinate when only the first is biased). The alkanes use the correct
nonlinear generalized mean force (including the geometric/curvature term) and
periodic estimators. In each case the FR step reallocates walkers along the reaction
coordinate by killing replicas in over-represented regions and cloning whole
configurations from under-represented regions, nudging the empirical marginal
$\phat_t$ toward a target $q_t$.

The central finding is not a single universal success story, but a regime map. In
the metastability model, where plain ABF already samples reasonably well,
estimated-target FR is a modest accelerator and the oracle diagnostic shows that
target direction can matter. In the cold entropic bottleneck, ABF is starved and
estimated-target FR gives a large gain ($+\EBgainStrong\%$, $\EBcoldWin/20$ seeds),
but the uniform target nearly matches it and the oracle target is worse than ABF;
here density correction and free-energy accuracy are not the same objective. A
dedicated entropy-dominant stress test (transverse dimension $m$, total barrier
held fixed) refines this further: as the barrier is made more entropic the
\emph{achievable} (oracle) gain grows specifically with the entropic share, yet the
\emph{deployable} estimated target does not capture it --- an entropic-specific
benefit that is real but target-limited. In
the WCA dimer, tuned FR again improves the free energy reliably
($+\WCAtunedGainPct\%$, $\WCAtunedWin/10$ seeds), while the best accuracy occurs
near the edge of an accuracy--diversity tradeoff. The molecular cases sharpen the
picture: in \emph{butane}, ABF already resolves the only slow coordinate, so mFR
turns itself off (its birth--death rate falls to $\approx\AlkButaneFrEvent$ and its
free-energy error matches ABF, \textbf{\AlkButaneVerdict} within a pre-registered
$\pm10\%$ practical-equivalence margin) --- the extra mechanism has no return where
it is not needed, rather than failing. In \emph{pentane}, where a second coupled
dihedral makes the conditional law $p(\varphi_2|\varphi_1)$ the binding difficulty,
we separate genuine improvement (better $F(\varphi_1)$ with faithful conditional)
from false improvement (flatter marginal but corrupted conditional), and report the
matched-seed outcome the data support rather than assuming the coupled case must be
positive. A final study varies the \emph{collective variable itself} to stress-test the
mechanism: replacing the easy torsion with a harder chemically meaningful scalar --- the
pentane end-to-end distance $R_{15}$ --- makes ABF genuinely sample-starved \emph{along the
biased coordinate} at low temperature (the first starved coordinate in the study), yet even
here mFR gives no improvement (equivalent at the deployable rate, monotonically worse and
lineage-collapsing as it is pushed), because that starvation is a \emph{dynamical} bottleneck
--- rare torsional-barrier crossings --- that marginal reallocation can only amplify, not
create; and promoting the hidden torsion into a genuine two-dimensional CV $(\varphi_1,
\varphi_2)$, with an exact vector generalized mean force and a periodic FFT Poisson
projection, removes the starvation entirely (ABF resolves both barriers directly), so mFR is
again neutral, now for a well-understood reason.

Two all-atom peptides extend the negative to real molecules and one constructed model
supplies the missing positive control. Vacuum \emph{alanine} and \emph{valine} dipeptides
were both selected as systems where mFR ought to help and both return
\textbf{\AlaVerdict}: valine's slow coordinate turns out to be the backbone $\varphi$ rather
than the side-chain rotamer it was chosen for --- its $11$--$18\,k_BT$ $\chi_1$ barrier is a
backbone-\emph{clamped} conditional barrier, worth $1.1$--$7.4\,k_BT$ once the backbone is
free --- and in both peptides ABF discovers \emph{and} establishes every state on its own, so
reallocation has no deficit to repair. Reading these together with the alkane results gives
regimes rather than a spectrum: \emph{ABF-sufficient} (found early, populated early;
mFR neutral), \emph{discovery-limited} (never found; mFR cannot act), and
\emph{establishment-limited} (found early, populated slowly), which is the only regime mFR
serves and the one that never arose on its own --- together with the
\emph{conditional-equilibration-limited} regime described below, which is checked first
because it decides whether the other three verdicts mean anything. A two-state reduction
explains the separation among the last three: discovery
is a first-passage event among $N$ walkers and takes $\sim 1/(N k_{01})$, whereas
establishment is a relaxation at rate $k_{01}+k_{10}$ with no factor of $N$, so more walkers
buy the first and not the second and the gap between them widens with $N$. We therefore build
that regime deliberately: an entropic-gateway model with analytic free energy, screened by
ABF alone over a $\GwCells$-cell \emph{finite-budget establishment map} whose classification
and anchor cell were frozen and committed \emph{before} any Fisher--Rao arm was run. On
$\GwcSeeds$ fresh seeds at rates frozen in a separate calibration pass, mFR improves the
integrated free-energy error by $\GwrEstGain\%$ $[\GwrEstCIlo, \GwrEstCIhi]$ on
$\GwrEstWins$ seeds, and beats \emph{its own} \textbf{matched sham} --- the same number of
clone/delete events at the same times, direction randomised --- directly by $\GwrDirect\%$
$[\GwrDirectCIlo, \GwrDirectCIhi]$. That run is a replicate on a corrected batch structure
and is the one the preregistration's amendment nominates for quoting; the first pass gave
$\GwcEstGain\%$ and $\GwcDirect\%$ and is neither withdrawn nor overwritten. An
estimator-independent frozen-bias endpoint, which
discards the online accumulators entirely, agrees at $\GwcFrozen\%$.

The same control is then carried to the many-body system. At the accepted WCA-dimer
configuration, on \WcsSeeds{} fresh seeds with no hyperparameter reopened, practical mFR
improves the integrated error by \WcvEstGain\% $[\WcvEstCIlo, \WcvEstCIhi]$ on
\WcvEstWins{} seeds and beats its own matched sham by \WcvDirect\%
$[\WcvDirectCIlo, \WcvDirectCIhi]$, while the sham itself is \emph{not resolvably} better
than ABF ($\WcvShamGain\%$, interval spanning zero on $\WcvShamWins$ seeds). These are scored against a corrected high-precision reference; the
cached reference used in the first pass was later found wrong by up to $\VtTroughSigma\sigma$
inside the transition region, which moved this headline from \WcsEstGain\% to
\WcvEstGain\% without changing any preregistered verdict. Paired on the same seeds, round trips move by
$\WcvRoundTripPct\%$ $[\WcvRoundTripCIlo, \WcvRoundTripCIhi]$ while the
error moves by \WcvEstGain\%, so in this setup the mechanism is consistent with
redistributing population among replicas already crossing rather than with buying barrier
crossings --- which is what a two-state reduction of the kinetics predicts. These are the first controls in
the study that separate Fisher--Rao steering from resampling of the same intensity and timing,
and they attribute the gain to the steering in both a designed model and a condensed-phase
molecular system.

Attribution to \emph{directed} selection is not the same as attribution to \emph{this}
direction, and the final experiment separates them. Run against the two directed rules already
in use --- the Chapter~6 Laplacian rule and simple count balancing --- at matched turnover on
$\QoneSeeds$ paired seeds with nothing retuned, mFR is \textbf{tied} by count balancing
($\QoneCountGain\%$, TOST interval $[\QoneCountTostLo, \QoneCountTostHi]$ inside a
$\pm\QoneTostMargin\%$ margin), which recovers the entire gain on its own
($\QoneCountVsAbf\%$ against ABF), while the Laplacian rule is inert ($\QoneBookVsAbf\%$).
The establishment-limited gain is therefore real, replicated and attributable to direction
rather than turnover, but the Fisher--Rao geometry is not what delivers it, and this report
claims a principled rule consistent with the mechanism rather than a win over prior art.

A structural result explains why a tie was the likely outcome and reorganises the map. For
\emph{any} score depending on the reaction coordinate alone --- which includes all three rules
above --- the mean-field selection flow satisfies $\partial_t\,p_t(y\mid\xi)=0$: it rescales
the marginal and leaves every conditional exactly invariant. Selection therefore cannot repair
a conditional that has not equilibrated, which is a fourth regime rather than a tuning
failure, and the map accordingly leads with a validity gate on the ABF baseline itself.
Deca-alanine is the cautionary instance: its discovery and establishment gates both pass, yet
independently prepared orthogonal ensembles disagree on the local mean force by $\GzDeca$ of
$|F'|$ (against $\GzWca$ for WCA and $\GzGateway$ for the gateway), so the classification that
would have made it a target for the method is inadmissible.

Two literature-anchored molecular systems were then entered as falsification benchmarks, both
preregistered with the stop condition written in advance. Methane association in water is
ABF-sufficient on $\MeSeeds$ of $\MeSeeds$ seeds, on a landscape whose barrier is
$\MeBarrierHeight\,k_BT$; sodium chloride association, which carries a genuine
$\NaBarrierKT\,k_BT$ desolvation barrier, is ABF-sufficient at both cells of its ladder that can
be classified at all. Neither licensed a Fisher--Rao arm, so the question of whether Fisher--Rao
directionality beats prior directed selection remains unanswered in a molecular system. The NaCl
null is the weaker of the two and is reported as such, on a criterion fixed before the data:
its hydration descriptors vary $\NaOrthoLo$--$\NaOrthoHi\times$ more across the coordinate than
within it, so ``there was nothing orthogonal for reallocation to act on'' survives as an
alternative to ``reallocation was not needed'', where methane's $\MeOrthoRatio\times$ leaves
real structure for the mechanism to have used. The regime the method serves has so far been
found only where it was built on purpose.

A cross-system audit over $\VmRuns$ runs, $\VmCells$ cells and $\VmSystems$ systems then asks
whether a practitioner could tell in advance when to switch the mechanism on. Under
leave-one-system-out validation no deployable ABF-only indicator predicts the sign of the gain:
the preregistered primary gate scores an out-of-sample AUC of $\VmGateAuc$
$[\VmGateCIlo, \VmGateCIhi]$ with a sign that flips between folds, and the only reliable
predictor is the measured ABF error itself, which presupposes the reference free energy the
method exists to estimate. The same audit shows the error budget is $\VmBiasFracAbf$ bias, so
marginal reallocation is a bias-reduction mechanism that costs a little variance rather than a
variance-reduction one.

Across all cases, FR is best understood as a
marginal-reallocation mechanism whose value depends on how starved the ABF estimator is
\emph{along the biased coordinate}, whether that starvation is a support deficit reallocation
can fill or a dynamical bottleneck it cannot, whether the conditional ensemble has equilibrated
at all, how quickly cloned orthogonal degrees of freedom
re-equilibrate, and how aggressively birth--death erodes ancestor diversity. The oracle target is
therefore a diagnostic control, not a deployable method and not a universal upper
bound.
