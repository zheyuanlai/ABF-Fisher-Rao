[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "rcwfr"
version = "0.1.0"
description = "Reaction-Coordinate Wasserstein-Fisher-Rao sampling for free-energy computation"
requires-python = ">=3.10"
dependencies = ["numpy", "scipy", "torch"]

[project.optional-dependencies-note]
# openmm is needed ONLY to build the alanine dipeptide parameters
# (src/rcwfr/mol/alanine.py); every other system is self-contained.


[project.optional-dependencies]
dev = ["pytest", "matplotlib"]
alanine = ["openmm"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
